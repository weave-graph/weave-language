# Acceptance gaps and source-reconciliation checklist

This audit covers the implementation at `62ca105` plus the documentation/tooling audit that follows it. **None of the full-design gates is complete.** Existing executable slices are evidence toward gates, not a substitute for the original scope. This file introduces no additional product requirements.

The original language and engine white papers have not been recovered. Available evidence is a partial private conversation excerpt and its delivery summary. No page numbers, conformance cases or exact syntax from the missing attachments are invented below. The named paper topics identify material to retrieve and review, not verified section headings. Publish recovered papers only after provenance and permission review; do not publish the private chat export as a substitute.

## Requirement-by-requirement gaps

IDs and original/proposed classification come from the [implementation plan](IMPLEMENTATION_PLAN.md). **Partial** means an executable subset exists. **Open** means no language implementation yet. Every row requires the original papers' relevant examples, failure cases and proposed conformance tests to be compared with the implementation before a conformance claim.

| ID | Status and current evidence | Missing acceptance behavior | Paper material / design decision / exact next tests |
|---|---|---|---|
| L01 | Partial: parser, AST, JSON diagnostics and basic graph checks | Schemas, rich types and AI-facing discovery | Recover grammar/schema/diagnostic proposal; ratify syntax and type rules; compile valid schema programs and verify invalid named fields/types with exact spans. |
| L02 | Partial: immutable named graph values feed filters and joins | General graph-returning functions, graph/function arguments and reusable modules | Recover lens/function semantics and purity boundaries; decide graph-value identity/lifetime; test higher-order composition and equivalence at identical snapshots without hidden commits. |
| L03 | Partial: typed relation/time parameters and partial binding | General typed parameter kinds and graph/function parameterization | Recover parameter/bind examples; specify evaluation and specialization rules; test wrong graph schema, partial higher-order application and parameter-dependent view identity. |
| L04 | Partial: exact entity-space path join | Arbitrary named binding joins, union and explicit resolution | Recover join/union/resolve rules; decide identity alignment policy; test independently named n-ary arguments, preserved disagreements and union without silent resolution. |
| L05 | Partial: valid intervals, valid-at filters and pinned system revisions | Full temporal operators and source-level corrections/history | Recover bitemporal/time-window semantics; choose timestamp/calendar/sequence operators; test late correction, historical replay, interval overlap/before/window and open ends. |
| L06 | Partial: distinct entity/space fields on manifestations | Protected counterpart discovery, identity membership snapshots and explicit cross-space operations | Recover multidimensional self-link model; decide identity-group and privacy semantics; test state isolation, hidden counterpart existence and offline-incomplete membership. |
| L07 | Partial: first-class binary edges and nodes | N-ary relation assertions and typed edge schemas | Recover assertion-versus-edge model and relation argument semantics; decide schema representation; test named n-ary arguments and metadata access across node/edge/assertion identities. |
| L08 | Partial: scalar properties and pinned graph-valued metadata; runtime traversal | Queryable recursive/shared metadata semantics across all cases; constructible cyclic references | Recover metadata reference, recursive-cycle and pinned revision design; decide stable revision identity versus content digest or transaction-local references; build a genuinely resolvable cycle, traverse under a budget, and distinguish denied/missing references. |
| L09 | Open | Automatic overlapping multilevel clustering and semantic zoom | Recover clustering/zoom proposal and stopping rules; select algorithms and budget semantics; test finite work at arbitrary requested depth, lineage, stable memberships and preserved anomalies. |
| L10 | Open | Typed 3D positions, vectors, frames, units and transforms | Recover geometry type rules; ratify frame/unit representation; test incompatible-frame rejection, time-varying transform composition and independence of edge direction from drawing position. |
| L11 | Open | Embedding-space descriptors, similarities and typed bridges | Recover encoder/version/metric and bridge semantics; decide exact/approximate modes; test equal vector length with incompatible encoders, lossy bridges and no identity inference from similarity. |
| L12 | Open in language; stored-query snapshot results exist | Federated references, peer querying and declared consistency | Recover distributed query coverage and snapshot design; choose remote consistency contract; test partial/unavailable peers, multiple revisions per graph and no global-atomicity claim from a revision vector. |
| L13 | Open | Distinct source-level attach/detach/replicate/fork/integrate effects | Recover capsule and attachment operation definitions; coordinate engine lifecycle APIs; test mount without acceptance, detach without erasing retained history and explicit merge policy. |
| L14 | Partial integration only: engine host authorization; source declarations public | Language permission/effect syntax and complete information-flow behavior | Recover capability and visibility proposal; decide effect typing and release policies; test private metadata/counterpart/count/explanation/geometry leakage and attempted authority escalation. |
| L15 | Open | Optional versioned governance and proposal acceptance | Recover governance decisions/policy transitions; choose exclusive-outcome coordination; test personal direct acceptance, reviewed proposal, stale policy and unauthorized policy escalation. |
| L16 | Partial: Rust core compiles to WASM target | Browser/mobile host APIs, real offline working sets, reconnection | Recover portable host and offline-branch requirements; choose supported device APIs; run disconnected edits/lenses on actual browser/mobile hosts, then synchronize with explicit unresolved dependencies. |
| L17 | Open in language | Typed subscriptions and explicit adapter/effect plans | Recover event bus and reactive syntax, failure/replay cases; coordinate engine envelope/cursor contract; test authorized subscription, replay, duplicate delivery and logical-effect idempotency. |
| L18 | Open | Typed contextual axes, applicability and explicit world crossing | Recover corrected space-versus-context semantics; ratify context composition; test omitted scope not universal, explicit broadcast and unauthorized world crossing. |
| L19 | Partial: exact entity+space key equality only | Versioned identity maps and candidate mappings | Recover identity alignment policy; decide mapping acceptance/ambiguity representation; test mismatched identifiers with accepted mapping, rejected ambiguity and map revision in provenance. |
| L20 | Partial: composed joins preserve exact leaf origins and input snapshots | General explanation graph, lens/schema revisions, source spans and dependency APIs | Recover provenance/explanation semantics; choose provenance schema and retention; test all derived forms, corrections and restriction propagation through explain output. |
| L21 | Partial: positive/negative edge claims retained | Four-valued proposition status, completeness assumptions and typed assessments | Recover contradiction/open-world/confidence rules; ratify proposition identity and assessment kinds; test unknown/supported/refuted/conflicted, absent versus false and no implicit probability arithmetic. |
| L22 | Open | Finite-domain recursive rules and resource-aware evaluation | Recover range restriction/negation/aggregation proposal; choose terminating core; test cyclic reachability, unsafe variables, negation cycles and budget exhaustion distinguished from no results. |
| L23 | Open in language | Hypothetical worlds, assumptions, branch masks and comparisons | Recover scenario semantics; separate branch lineage from world representation; test baseline preservation and retained assumptions with no implied causal claim. |
| L24 | Open | Materialized/live lenses and rolling windows | Recover live-view event/time/support-delta design; choose incremental model; compare incrementally maintained output with recomputation after insert, correction, expiry and removal. |
| L25 | Partial: engine complete/partial coverage preserved in values | Full local-fragment/unknown/denial/unsupported coverage distinctions | Recover result manifest categories and privacy rules; choose disclosure-safe diagnostics; test empty complete versus unavailable versus unauthorized without hidden-existence leakage. |
| L26 | Partial: program-local immutable bindings and deterministic derived edges | Stable referenceable view identity including plan, parameters, schemas and snapshots | Recover graph-view identity semantics; ratify canonical encoding and semantic hash inputs; test identity stability and change for every relevant dependency. |
| L27 | Partial: data/code separation and no executable host-authority fields | General effect type system, validated source references and proposal promotion | Recover explicit effect/source validation design; decide trusted host interface; test ingestion attacks, forbidden pure-lens effects and authorized promotion separated from structural validation. |
| L28 | Open | Exact/approximate query modes and sound multiscale pruning | Recover approximation/coverage/zoom guidance; decide proof obligation for pruning; test approximate routing never silently changes exact answers and all approximations remain declared. |
| L29 | Partial: check/ast/plan CLI and machine-readable diagnostics | Schema `describe`, formatter and richer tooling | Recover discovery/diagnostic examples; define discovery manifest; test schema discovery and repair workflow. LSP is an optional proposed convenience, not a blocker for user-requested semantics. |
| L30 | Partial: MIT source, locked builds, CI definition and source distribution checks | Verified public GitHub repositories/CI, full release audit and reproducible public artifacts | No missing paper is required to establish public GitHub availability; orchestrator must verify organization/repositories and CI URLs. Registry packaging is optional and separate. Full-design release requires all required rows above and measured limits. |

## Cross-project acceptance still missing

The current composed-value fixture proves a useful narrow path: source graphs → temporal join → parameterized filter → second join → final graph with leaf provenance. It does not prove offline/mobile behavior, live views, general reasoning, recursive metadata cycles, full authorization, clustering, geometry or governance.

The complete offline-phone story still needs evidence-graph editing, offline local execution, authorized reactions, portable history exchange, conflict preservation, explicit governance acceptance and explainable accepted outputs. Duplicate events, unavailable dependencies and revoked authority must be tested in the same end-to-end scenario. Engine-only capsule tests must not be reported as language or mobile conformance.

## Public-release readiness versus full-design completion

A public experimental GitHub source release can proceed with clearly documented gaps, MIT notices, reproducible source builds and passing public CI. It does not require pretending the full design is complete. The original requested project completion remains open until the relevant requirements pass their gates or the owner explicitly amends scope.

`cargo package`/crates.io publication is not the current distribution path: Cargo excludes nested package contents and strips path dependencies when packaging. This repository is marked `publish = false` until a coordinated optional registry release exists. Git clones and `git archive` include the standalone vendored contract and can build or install with `cargo install --locked --path .`. Do not describe a `.crate` package as self-contained.

The audit does not establish a minimum supported Rust version below the tested 1.94.0, browser runtime support from a WASM check, success on an unrun CI platform, or a configured private security-reporting channel. Those are explicit evidence gaps, not implied capabilities.
