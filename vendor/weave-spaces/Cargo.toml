[package]
name = "weave-spaces"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Bounded pure geometry with explicit spaces, time, lineage and restrictions"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
weave-contract = { path = "../weave-contract" }
