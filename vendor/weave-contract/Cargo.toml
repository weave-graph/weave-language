[package]
name = "weave-contract"
version = "0.3.0"
edition = "2021"
license = "MIT"
description = "Portable versioned Weave language-runtime contract"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
